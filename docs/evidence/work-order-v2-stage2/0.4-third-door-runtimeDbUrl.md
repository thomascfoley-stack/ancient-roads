# Tranche 0.4 addendum — third second-door instance: runtimeDbUrl (2026-07-31)

**PROPOSE ONLY. No general mechanism implemented.**

## Context

`docs/evidence/work-order-v2-tranche0/0.4-second-door-report.md` catalogued every repo path that
can reach `ep-odd-fog`. Three instances of the same defect class have now been closed **locally**
(without implementing the proposed §3a `db-open.mjs` chokepoint):

| # | Instance | Surface | Fix (this branch) |
|---|----------|---------|-------------------|
| 1 | `scripts/unit-ordinal-instrument.mjs` | CLI minted via `NEON_API_KEY` | Tranche 0.1 — no transpiler spawn, server-side read-only assert |
| 2 | `node -e` with pasted URL | **Uncloseable in git** (0.4 §2, C1) | Not fixed — honesty preserved |
| 3 | `web/test/helpers/env.ts` `runtimeDbUrl()` | db-invariants behavioral suites via `APP_DATABASE_URL` / `DATABASE_URL` | **Tranche 3** — prod endpoint refused loudly (mirrors `seedOwnerUrl`) |

## What changed

`runtimeDbUrl()` previously returned any configured URL with no prod guard. Seeding suites already
had `seedOwnerUrl()` refusing `ep-odd-fog`; runtime behavioral suites (tenancy, section-vector-pairing,
work-reader DB leg, unit-ordinal published leg) did not.

**Property now enforced:** no test leg can connect to production via `APP_DATABASE_URL` or
`DATABASE_URL`. Red-proof: `web/test/invariants/seed-owner-url.test.ts` `runtimeDbUrl` describe block.

## What is still open

- **C1/C2** (`node -e`, raw `psql`) — unchanged; only Neon role split (0.4 §3c) makes prod writes impossible.
- **B3 ingest scripts** — still env-driven with no in-script guard; `assert-ingest-env-dev.mjs` runs only in audit.
- **§3a chokepoint** — still the recommended repo-wide fix; these three patches are point guards, not the mechanism.

## Recommendation (unchanged from 0.4 §3)

Land **§3b static test** (`new pg.Client` grep) first to get the true call-site count, then
`db-open.mjs` on its own branch. Do not fold into behavioural work orders.
