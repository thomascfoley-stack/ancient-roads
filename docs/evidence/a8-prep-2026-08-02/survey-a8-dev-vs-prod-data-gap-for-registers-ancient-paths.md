# Survey: A8 dev-vs-prod data gap for registers (Ancient Paths)

## Gate definition
`docs/pm/MASTER.md:44`: "A8 | Register ingest slice → Deploy B → publish registers | Blocked on A7". That single row is the entire filed A8 plan; no A8 order exists under `docs/pm/orders/` (bylaw 1, `MASTER.md:12`: not in repo = never issued). "Deploy B" is defined nowhere else (A6 is "Deploy A", `MASTER.md:42`) - NOT ESTABLISHED beyond the row.

## What dev has (most recent committed census)
The last full per-register dev census is **2026-07-19** (`WORKLOG.md:1322-1338`, measured on `ep-tiny-hat`, `status='published'` AND sections rows): sermon **7 works / 162,827 sections**; theology 3/29,050; father 3/18,472 (+1 staged/1,224); confession 1/5,437; poetry 10/3,543; hymn 5/1,690; commentary 2/27,283; historian 0 published (3 staged/4,124); lexicon 0 published (5 staged/52,043). Quarantined/excluded: barnes-notes 1,300, donne, herrick (`WORKLOG.md:1338`).

Flat-store side (2026-07-18 full re-ingest, `WORKLOG.md:2121-2128`): "34 works served... **297,059 register rows**; all 6 indexes valid; FTS 191,749 rows" - WORKLOG prose, no evidence artifact under `docs/evidence/` for these numbers. The 7/19 repoint sweep sliced "all 33 register works... ~274k flat rows" into the sections model, 1:1 per work (`WORKLOG.md:1677,1756`). Dev commentary flat embeddings separately = 190,635 (`docs/STATE_OF_TRUTH.md:151`). Reconciling 297,059 vs ~274k vs the census section totals is NOT ESTABLISHED anywhere. On 2026-07-31, **61,486** dev sections got `unit_ordinal` repaired across 7 register works (`STATE_OF_TRUTH.md:183-196`) - a count STATE_OF_TRUTH itself marks **UNVERIFIED**, resting "on the tool's own log" (`STATE_OF_TRUTH.md:242-245`). No dev census postdates 7/19; A8 planning starts 13 days stale.

## What prod has
**Schema/indexes: everything. Data: nothing.** Migrations 017-020 all applied to prod in the 2026-07-29 E1 (`docs/evidence/cutover-2026-07-29/prod-E0-E6.log:123,133` = 017; `:134,144` = 020; `:255,281` = 018 concurrent; `:282,300` = 019 concurrent; summary `STATE_OF_TRUTH.md:107`). So prod has the register source_type CHECKs (017/020), register-aware partial HNSW indexes (018), and `commentary_entries.work/register` columns + FTS legal partial (019, whose predicate lists olney-hymns, spurgeon-sermons, etc. - `db/migrations/019_register_columns_fts.sql:28`). But:
- G5 register wall on prod is **VACUOUS - 0 lane/song slug rows ("register ingest never on prod")** (`STATE_OF_TRUTH.md:116`; log lines 89,767; `WORKLOG.md:180`).
- E2 labeled 77,820 of 190,635 flat rows; 112,815 unlabeled (`STATE_OF_TRUTH.md:109`); all labeled rows are commentaries (`docs/D2_PROD_RUNTIME_PROJECTION.md:63`).
- A2 census 2026-08-01 (`docs/evidence/a2-prod-readonly-2026-08-01/serving-census.md`, measured read-only): 7 sources, all commentary, 72,863 sections; "**No production source is in either list**" (SERVED_PROSE_WORKS/SERVED_LANE_WORKS); keil-delitzsch, catena-aurea, chrysostom-homilies, augustine-homilies "none... exists on production at all"; served pool = **9 authors (83,993 rows), not dev's 11**.
- Tonight's flip (context; `MASTER.md:39-40`): 6 works published via `scripts/publish-flip.mjs`, barnes-notes stays staged.

## What has to move, per the docs
Prod cannot self-build registers the way it built commentaries: E4 sliced sections by **reusing prod's own flat vectors 1:1 at $0** (`D2_PROD_RUNTIME_PROJECTION.md:65`), but prod's flat pool contains zero register rows, so there is nothing to re-point. The only committed plan statement is the pre-cutover C3 (`docs/GO_LIVE_EXECUTION.md:52`): "Run the ingest against prod (idempotent, ON CONFLICT DO NOTHING)... ⛔ **Never branch-promote the dev Neon branch onto prod** - that... wipes live user data... **A fresh re-ingest (paying the re-embed cost) is the only safe path.**" Echoed at `WORKLOG.md:1233` ("re-ingest register works dev has but prod lacks"). C3 never ran; the E0-E6 cutover explicitly excluded register ingest (`WORKLOG.md:109`: "register ingest - explicitly not started (hard rail)"). A row-level copy-from-dev preserving vectors is documented nowhere - NOT ESTABLISHED as an option.

Tooling gap: `src/ingest/register-writer.ts` (the ONE register writer, `WORKLOG.md:2363`) refuses prod by construction - `NEON_BRANCH` must be dev|test (`:161`) AND the URL must match `ep-tiny-hat|localhost|127.0.0.1` (`:165`). Same class as the A4 gap (`MASTER.md:40`: "no safe prod writer"), which was solved by the purpose-built `scripts/publish-flip.mjs`; A8 needs the equivalent for ingest.

## DeepInfra / cost implications
`register-writer.ts` calls DeepInfra directly: `DEEPINFRA_API_KEY` required (`:158-159`), `embedRaw` POSTs to `api.deepinfra.com/v1/openai/embeddings` (`:65-75`), chunks capped at `REGISTER_EMBED_MAX = 1200` chars (`:19`). So a C3-style fresh prod re-ingest re-embeds every register chunk (plus re-fetches sources via the adapters). Only cost anchor committed: `docs/MIGRATION_DESIGN.md:27` - re-point $0 vs re-embed "~$0.74+ and rising" for ~170k units; no register-scale (~297k rows) projection exists - NOT ESTABLISHED. Note also DeepInfra flakiness is a known gate hazard (`MASTER.md:127-132`: 429 `engine_overloaded` produced an unearned CI red). Vector-copy slicing (no API) runs at the measured 121-190 s/10k rate (`D2:65`).

## Measured vs asserted
Measured with committed artifacts: prod A2 census; E0-E6 log rows; migrations 017-020 applied. Measured-but-prose-only: 7/19 dev register census (WORKLOG table). Asserted/tool-log-only: 297,059 register rows; 61,486 repair count (flagged UNVERIFIED); dev end-state freshness.

## Biggest unknown
**How dev's curated register corpus is reproduced on prod.** The docs' only sanctioned path is fresh re-ingest from sources, but dev's state is ingest **plus** curation - ADR-029 suppressions, non-authorial-matter deletes, quarantine rulings, the 33-work repoint, the 7/31 unit_ordinal repair. No document says how a fresh prod re-ingest replays that curation (the suppression scripts are hard-guarded to `ep-tiny-hat` only, `STATE_OF_TRUTH.md:176-178`), what it costs, or how "prod equals dev" would even be asserted (CUTOVER_DESIGN.md:125-127 forbids hardcoding dev counts). Resolving re-ingest-vs-copy, and the guarded prod writer it implies, is the A8 planning decision.
