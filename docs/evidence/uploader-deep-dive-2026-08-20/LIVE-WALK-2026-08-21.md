# Signed-in live walk of deploy 9567a88 — external verifier, 2026-08-21

Conducted by the owner's Kimi K3 session, signed in against production (`ancientpaths.app`,
deploy `9567a88`), relayed by the owner. Filed here because it discharges browser-DoD legs this
repo's agent sessions could not run (no credential entry), and leaves a precise open list.

## What it verifies, cross-referenced

| Claim | Verdict | Closes |
|---|---|---|
| My Works: 663-char search query → API 400s, page renders "That search is too long. Please shorten it." **and stays alive** | **VERIFIED LIVE, signed in** | B020's live leg (the page-blanking crash — `dd13122`'s WORKLOG NOT-DONE "no browser look" is now half-closed: the error path is walked; the Export menu look remains) |
| Historians "Open in book →" carries `#s17` — a real ordinal — and lands at the section | VERIFIED LIVE | The deep-link CRITICAL fix (`ec4aa54`), previously verified by author + auditor only |
| Daily Office on /home, plan URLs, catch-up card with Resume/Keep-dates | VERIFIED LIVE | Sprint 1's browser leg |
| Highlighter full loop incl. tap-word → Greek (σὰρξ/G4561) | VERIFIED LIVE | — (predates this dossier) |
| History honesty strip on zero entity match | VERIFIED LIVE | — |

## What it could NOT verify, stated

- **B019 (search returned one result) and B022 (stuck-document recovery): NOT exercised live** —
  the walking account holds zero documents. Both remain covered by tests + the dev measurement
  (1 vs 20 results; reclaim suite) only. First account that uploads a sermon closes them.
- The Studies Export menu (markdown removal, `dd13122`) postdates deploy `9567a88` — **not yet
  deployed**, so not walked.

## Flags raised by the walk (owner's list, standing)

1. **Similarity floor still open** — nonsense queries get a confident "Closest match" hero in
   history mode. Held safe only by the site gate; pre-public-launch blocker.
2. **Entity recall thin** — "Ephesus" matched no known place; consistent with the morning file's
   50-of-81 out-of-scope finding (watchlist instance 17).
3. Housekeeping: a leftover sky highlight on John 1:5 sits in the owner's account (the walker
   could not complete the remove gesture); the 5 `DELETE /api/research/*` calls mid-walk were the
   owner's own thread cleanup, not a defect.
