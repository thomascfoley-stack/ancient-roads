# Migration 117 applied — dev and production, 2026-08-18

Owner-directed ("ok do it"). `db/apply-migration-concurrent.mjs`, zero-window rebuild.

| target | result | ledger |
|---|---|---|
| dev `ep-tiny-hat` | 3/3 parts, index VALID+READY | `sha256 bd51593eb946…` |
| **prod `ep-odd-fog`** | 3/3 parts, index VALID+READY | `sha256 bd51593eb946…` |

Same sha on both — the same file, applied twice, which is the point.

## Verified on production after the apply

| check | result |
|---|---|
| veto in the LIVE index predicate (`pg_get_expr`) | YES |
| name-prefix rule present | YES |
| index VALID and READY | true |
| **planner uses the index, not a seq scan** | **YES** |
| vetoed authors reachable through the predicate | **0** |
| rows still admitted | 64,216 |
| stray `_v11` index left behind | none — renamed cleanly |

**The seq-scan check is the one that mattered.** The whole reason this needed a migration is that the
constant IS the partial index's predicate; had they drifted, results would be correct and the
surface would silently scan. Confirmed on prod with `EXPLAIN`.

## Delta on production is zero, proven by construction rather than by slicing SQL

Two facts settle it without reconstructing the old predicate:

- `commentary_entries.work` is **0 non-NULL of 371,406** on prod, so the work-slug leg of the
  allowlist admits nothing at all;
- **no vetoed author appears on the 8-name allowlist** (nor is any Chrysostom/Augustine).

So no vetoed row could ever have satisfied the allowlist. Nothing that was served stopped being
served. **32,407 rows** move from "excluded because unnamed" to "excluded because named and refused".

## Prod carries variants lane-b does not — which is why prod was checked and not extrapolated

Vetoed (12 strings): Tyndale Study Notes 15,161 · Theophylact of Ohrid 5,715 · Origen of Alexandria
2,205 · Oecumenius 1,828 · CS Lewis 1,102 · Theophylact-via-Aquinas 930 · GK Chesterton 714 ·
Origen-via-Aquinas 467 · `CS Lewis  (via the character Screwtape, a devil)` 70 · Douglas Wilson 16 ·
JRR Tolkien 11 · Origen-referenced-by-Jerome 1.

Lane-b showed Theophylact at **11**; production has **5,715**, plus the Aquinas-quotation forms.
Extrapolating the lane-b numbers to prod would have been wrong by an order of magnitude.

**Three strings pass the veto, and two of them SHOULD:**

| author string | rows | vetoed? | assessment |
|---|---|---|---|
| `Ignatius of Antioch  (as quoted by Origen, AD 235)` | 1 | no | **correct** — Ignatius is a legitimate father; "Origen" is only in the attribution |
| `Heracleon  (as quoted by Origen, AD 253)` | 1 | no | **correct** — same shape |
| `Pseudo-Origen  (as quoted by Aquinas, AD 1274)` | 12 | no | **RESIDUAL — owner call** |

The first two are the rule declining to over-reach, which is the property the space-anchored prefix
was written for. The third is a real gap: `Pseudo-Origen` is not a listed name, no first-token or
prefix rule reaches it, and it remains excluded only by the weak mechanism (absence from the
allowlist). Whether the Origen ruling extends to a pseudonymous attribution is an editorial call and
is NOT made here.
