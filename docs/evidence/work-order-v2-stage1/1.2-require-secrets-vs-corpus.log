# 1.2 follow-up — REQUIRE_SECRETS vs gitignored artifacts (PR #44)

## Direction A: missing secret -> FAILURE

```
REQUIRE_SECRETS=1 + announceSkip({ name: 'FAKE_SECRET', present: false })
-> throws REQUIRE_SECRETS: ... FAKE_SECRET
```

(vitest test/invariants/loud-skip.test.ts)

## Direction B: missing corpus artifact -> LOUD SKIP (db-invariants GREEN)

Corpus dir temporarily absent, REQUIRE_SECRETS=1:

```
↓ test/invariants/verse-keys.test.ts (2 tests | 2 skipped)
⚠  NOT RUN — §3 verse-key distribution DID NOT RUN — missing web/public/commentaries (gitignored static corpus)...
exit=0
```

## Direction C: missing corpus -> predeploy-gate RED

```
✗ PRE-DEPLOY GATE FAILED
The static commentary corpus is missing:
  .../web/public/commentaries
exit=1
```

REQUIRE_CORPUS=1 on verse-keys (for Stage 3.1 / predeploy) throws at module load when corpus absent;
predeploy-gate fails before deploy today via existsSync(COMMENTARIES_DIR).
