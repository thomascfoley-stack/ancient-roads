# Content quarantine: one served Origen entry removed — 2026-08-03

**Not a new ruling.** Origen is already must-not-serve per the A6 ruling (`docs/DECISIONS.md`,
2026-07-17, "Origen (origen-commentary): excise"). This is enforcement, executed after the
predeploy gate refused a deploy on it.

## Found

`web/src/lib/predeploy-gate.ts`'s must-not-serve scan over delivered static bytes (not rendered
output) flagged:

```
1 SERVED entries carry an author that must not be delivered.
  1 entries  0.01 MB  [must-not-serve]  Origen   e.g. web/public/commentaries/1ti/2.json
```

## Swept

`grep -rl '"author":"Origen"' web/public/commentaries/` — exactly one file, one entry.

```json
{"verseStart":9,"verseEnd":9,"author":"Origen","tradition":"unassigned",
 "sourceTitle":"Origen on Prayer","sourceUrl":"https://www.ccel.org/ccel/origen/prayer",
 "text":"Chapter V. Answer To ..."}
```

## Removed

`web/public/commentaries/1ti/2.json`: 208 entries -> 207. Compact JSON (no pretty-print),
byte-identical otherwise. File is a symlink shared across worktrees, so this is the one and only
copy on this machine.

## Verified

`DEPLOYING=1 npx tsx scripts/predeploy-gate.ts` re-run: the must-not-serve section no longer
lists Origen; the licensing ratchet holds at 0 forbidden-provenance entries. The gate's next
failure (corpusHash manifest mismatch) is unrelated corpus growth from concurrent ingest work
(149,315 -> 157,888+ entries between this run and the last committed manifest) and is handled at
deploy time by regenerating the manifest against the exact tree being shipped.
