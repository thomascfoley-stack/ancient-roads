# Orders and verdicts

Per `docs/pm/MASTER.md` bylaw 1: if it is not in this directory (or under `docs/pm/`), it was
never issued. Chat-window decisions do not exist.

| Kind | Naming |
|------|--------|
| Order | `YYYY-MM-DD-<slug>.md` |
| STOP audit prompt | `YYYY-MM-DD-stop-audit-<stage>.md` |
| STOP audit verdict | `YYYY-MM-DD-stop-verdict-<stage>.md` |
| Owner decision | `YYYY-MM-DD-decision-<slug>.md` |

Gate board: [`../MASTER.md`](../MASTER.md).
